# Package Note

An earlier package included:
- blueprint
- schema plan
- build plan
- API influence map
- MVP cut
- research and source registry

The updated master package in this repository preserves the current direction for:
- the internal Decision Intelligence Console
- the external product Shortlist
- the architecture, roadmap, feature map, agent system, and source registry that connect them

This note exists as a lightweight bridge between the older package summary and the newer searchable docs now stored in the repository.
